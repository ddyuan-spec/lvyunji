# 图生图商业化及 AI 工具规划 PRD

> 文档全称：**图生图商业化及 AI 工具规划 PRD**（V0.5）。
> 范围：两部分——① **图生图商业化**（积分体系：购买 / 赠送扣除 / 初始值 / 成本配置 / 生成日志 / 后台会员管理对齐）；② **AI 工具演进规划**（素材库 / 生成视频 / 数字人分身 / 风控律师 的时间推进与完成度定义）。
> 与 V0.4 关系：V0.4 = 我的作品 + AI 生图 + 素材库（已交付）；V0.5 = 在 V0.4 之上补足**积分管理体系**并规划后续 AI 工具演进路径。
> 命名约定：本 PRD 与 C 端界面统一使用「**积分**」作为对外展示文案；数据库表 / 接口字段保留 `ai_credit` 物理命名避免大改。
> 变更说明：V0.5 评审将对外文案由「额度」统一改为「积分」（用户 2026-09-01 定稿）。

---

## 一、文档基本信息

| 版本 | 日期 | 修改人 | 修改原因 |
|------|------|---------|------------|
| V0.1 | 2026-08-18 | 产品 | 初版：AI 生图/生视频核心功能 + 广场/做同款 + 历史记录 |
| V0.2 | 2026-08-20 | 产品 | 评审精简：去 AI 生视频、创作广场、做同款；合并 AI 创作首页 + 我的作品为单入口页（我的作品）；AI 生图去除模型选择、画面比例仅保留 9:16/16:9；结果弹窗仅保留 分享 + 保存到相册；分享复用《我的》PRD §4.4 分享弹窗（海报主视觉/文案按 AI 场景改造）。详见 PRD 内各章节评审说明。 |
| V0.3 | 2026-08-21 | 产品 | 评审修订：① 画面比例去除用户选择，固定生成比例 311:360；② 生成记录点击改为底部弹窗打开生成结果（复用现有生成结果页）；③ 积分购买与积分明细归后端设计，前端不实现、不展示对应入口与字段。 |
| V0.4 | 2026-08-21 | 产品 | 新增「素材库」板块：数商服务首页素材库入口跳转文件列表页，支持素材预览与下载；后台同步新增素材管理上传能力。 |
| **V0.5** | **2026-09-01** | **产品** | **图生图版本迭代：补足积分管理体系。C 端新增「积分购买」（不做退款，二次弹窗「购买后不支持退款」）+ 「积分明细」+ 「余额不足」前置校验；后台新增「会员管理列表加列/筛选项/操作按钮」「用户生成日志」「初始用户积分配置」「生图成本配置」「调整积分弹窗（赠送/扣除）」。**命名由 V0.4 的「额度」统一改为「积分」**（用户 2026-09-01 定稿）：对外展示文案全部用「积分」，数据库表／接口字段保留 `ai_credit` 物理命名避免大改。AI 生图页新增「本次生成将消耗 N 积分」提示，N 取后台生图成本配置（§4.9）。** |

---

## 二、项目背景与目标

### 2.1 业务背景

V0.4 已完成"能生成图片 + 素材库能看能下"，但**没有积分管理能力**：用户积分只读展示、购买/明细归后端、初始值与生图消耗无配置。运营侧无法：(1) 控制生图成本；(2) 调节新用户初始值；(3) 给特定用户赠送或扣除积分；(4) 追溯每次生成的积分流水。本期一次性补足。

### 2.2 项目目标

- **业务目标**：让运营可配可查可控积分，让用户能自助购买补足积分但不依赖客服；为后续视频（V0.7）打好积分账户基础设施。
- **用户目标**：(1) C 端用户在积分不足时能一键充值；(2) 充值前明确知道"购买后不支持退款"；(3) 随时查看积分流水。

### 2.3 衡量指标（KPI）

| 指标 | 目标 | 说明 |
|------|------|------|
| 积分购买转化率 | ⚠️ 待确认 @运营 | 看到「积分不足」弹窗后 7 日内完成购买的用户占比 |
| 积分赠送人均次数 | ⚠️ 待确认 @运营 | 后台运营每周主动赠送积分的人次 |
| 用户生成日志查询次数 | ⚠️ 待确认 @运营 | 运营每周查询用户生成日志的次数 |
| 退款工单 | 0 | 本期不做退款流程，如产生人工退款诉求走线下 |

---

## 三、业务流程与架构

### 3.1 本期范围与影响

| 端口/模块 | 本期范围 | 影响说明 |
|-------------|------------|------------|
| **C 端 - 我的作品** | 积分账户卡片改造：保留「剩余积分」「我的作品数」两列展示；新增「购买积分」「积分明细」两枚按钮 | V0.4 已交付页面增量改造 |
| **C 端 - 积分购买**（新增） | 套餐选择 → 下单 → 支付 → **二次弹窗"购买后不支持退款"** → 成功到账；本期不做退款流程 | C 端新增独立页 |
| **C 端 - 积分明细**（新增） | 顶部「充值」入口 + 账户卡片（剩余/本月已用/累计充值）+ Tab 全部/消费/充值 + 流水列表 | C 端新增独立页（与设计稿一致） |
| **C 端 - AI 生图** | 积分为 0 时前置拦截：toast/弹窗「积分不足，请先充值」+ 一键跳转充值页 | V0.4 既有页面加前置校验 |
| **后台 - 会员管理**（改造） | 列表新增「积分余额」列；新增筛选项（=0 / 区间 / 排序）；新增操作按钮「调整积分」 | 截图所在页改造 |
| **后台 - 调整积分**（新增） | 弹窗：赠送（数值+备注）/ 扣除（数值+备注，扣到 0 提示） | 会员管理列表联动 |
| **后台 - 用户生成日志**（新增） | 用户/时间/内容类型/提示词/消耗积分/结果 URL/状态；筛选 + 导出 | 后台新增独立页 |
| **后台 - 初始用户积分配置**（新增） | 积分值 + 生效日期 + 设置人 + 备注 + 历史快照 | 后台新增独立页 |
| **后台 - 积分定价管理**（新增） | 积分单价（元/积分）+ 生效日期 + 设置人 + 备注 + 历史定价时间线 | 后台新增独立页；套餐价格 = 积分 × 当前有效定价 |
| **后台 - AI 工具消耗配置**（新增） | 按工具类型（图片生成/视频生成/数字人分身）各自配置消耗积分数 + 生效日期 + 设置人 + 备注 + 历史快照 | 后台新增独立页；替代原「生图成本配置」单表，扩展为多工具支持 |
| 视频（V0.7） | **不在本期** | 后续版本独立 spec，积分账户复用本套 |

### 3.2 业务流程图

#### 3.2.1 积分购买流程（C 端）

**定价模型**：套餐只定义「送多少积分」，价格 = 积分数 × 后台当前有效 `price_per_credit`（元/积分）。运营改定价即全站生效，无需逐个改套餐。

**新人首单优惠**：首次购买（`is_first_purchase = true`）触发「买 N 送 N」——实付积分数额外等量赠送，仅限一次。

```
我的作品 → 点击「购买积分」 → 套餐选择页 → 选择套餐 →
  ├─ 价格自动计算：套餐积分数 × 当前有效 price_per_credit
  ├─ 新人首单显示：「首单再送 X 积分」（X = 套餐积分数）
  → 下单 → 支付 →
┌──────────────────────────────────────────────────────────────────┐
│  支付成功 → 二次弹窗 "购买后不支持退款，请确认"                  │
│  （弹窗内展示：购买 X 积分 · 支付 ¥Y · 新人首单额外赠送 X 积分） │
│  → 用户确认 → 到账                                              │
│  └→ 用户取消 → 订单保留为"未到账"，人工介入                      │
└──────────────────────────────────────────────────────────────────┘
→ 积分账户余额 +N（+ 首单额外 +N）→ 写入积分流水（来源：购买）
→ 购买订单快照当时的 price_per_id（用于对账，后续改价不影响本单）
→ 我的作品页余额刷新
```

异常分支：
- 支付失败：toast「支付失败，请重试」；订单状态置为"失败"，不扣积分
- 二次弹窗取消：订单状态置为"已支付但未到账"，触发后台通知运营，运营侧手工补单（V0.5 不实现自动退款/补单流程，PRD §6 写明）
- 网络中断：支付中间状态由前端「订单状态机」保护，二次进入购买页时弹窗提示「存在未完成订单，是否继续」

#### 3.2.2 余额不足拦截流程（C 端 AI 生图）

```
AI 生图页 → 用户点击「立即生成」→
  ├─ 积分 ≥ 1 → 校验通过 → 调用 Coze 生图 → 扣积分（消耗 N）→ 写入积分流水
  └─ 积分为 0 → 弹窗「积分不足，请先充值」+ 两枚按钮
                  ├─ 「立即充值」→ 跳转积分购买页（保留原提示词）
                  └─ 「取消」→ 关闭弹窗回到生图页（提示词保留）
```

#### 3.2.3 后台积分管理流程

```
运营 → 会员管理列表 →
  ├─ 筛选（按余额 =0 / 区间）→ 找到目标用户
  ├─ 点击「调整积分」→ 弹窗
  │    ├─ 赠送：输入数值 + 备注 → 提交 → 余额 +N → 写入积分流水（来源：后台赠送）
  │    └─ 扣除：输入数值 + 备注 → 提交
  │          ├─ 积分 ≥ 输入值 → 余额 -N → 写入积分流水（来源：后台扣除）
  │          └─ 积分 < 输入值 → 二次确认弹窗「将扣除至 0，确认？」→ 用户确认后继续
  ├─ 点击「生成日志」→ 跳转该用户的生成日志详情（默认带 user_id 筛选项）
  └─ 点击其他列的既有操作（查看/修改手机号等）→ 沿用既有逻辑

运营 → 初始用户积分配置 →
  设置积分值 + 生效日期 → 保存 → 写入配置历史快照（valid_from）→
  └─ 后续注册的新用户按 valid_from ≤ 注册日 的最新配置一次性发放
  └─ 已注册的老用户不动（不追溯、不冻结）
```

#### 3.2.4 生图成本生效流程

```
运营 → 生图成本配置 → 设置每张图扣 N 积分 + 生效日期 → 保存 →
  历史快照保留 + 当前成本按 valid_from 取最新一条
  └─ 用户调用生图时取该最新值计算扣减
  └─ 已消耗的积分不回溯（不重算历史生成的成本）
```

### 3.3 信息架构

| 层级 | 模块/数据 | 说明 |
|------|-------------|------|
| **C 端** | 我的作品（沿用 V0.4 + 加购买/明细按钮）、积分购买页、积分明细页、AI 生图页（加余额不足拦截） | V0.4 之上扩展 |
| **后台** | 会员管理列表（加列/筛选项/操作按钮）、调整积分弹窗、用户生成日志、初始用户积分配置、积分定价管理、AI 工具消耗配置 | 本期新增 |
| 接入层 | 绿韵家服务端 API | 新增：`/credit/account`、`/credit/purchase`、`/credit/purchase/confirm`、`/credit/detail`、`/credit/init-config`、`/credit/pricing`（当前有效定价）、`/credit/tool-cost`（工具消耗配置）、`/credit/adjust`、`/credit/log`、`/generation/log` |
| 数据层 | ai_credit_account（账户余额）、ai_credit_log（流水）、ai_credit_init_config（初始值配置 + 生效日期）、**ai_credit_pricing（积分定价：price_per_credit + effective_from + 历史快照）**、**ai_tool_cost_config（工具消耗配置：tool_type + cost_credits + effective_from + 历史快照，替代原 ai_image_cost_config 单表）**、ai_purchase_order（购买订单，含 pricing_snapshot_id 快照定价）、ai_generation_log（生成日志，含 cost_config_snapshot_id 快照消耗） | ⚠️ 待确认 @研发：表名与字段；本期复用 V0.4 的 ai_generation_log 表，**新增消耗积分字段 credit_cost** |

### 3.4 状态机与状态流转

#### 3.4.1 购买订单状态机

| 起始 | 动作 | 前置 | 目标 |
|------|------|------|------|
| 积分为0 | 进入 AI 生图页 | 已登录 | 提示积分不足 |
| 积分 ≥ 1 | 点击立即创作 | 提示词非空 | 参数配置中 |
| 参数配置中 | 点击生成 | 积分 ≥ 1 | 生成中 |
| 参数配置中 | 点击生成 | 积分为 0 | 提示积分不足并拦截（无购买入口时的状态） |
| 我的作品 | 点击购买积分 | 已登录 | 套餐选择 |
| 套餐选择 | 点击购买 | 已选套餐 | 待支付 |
| 待支付 | 支付回调成功 | — | 已支付（中间态） |
| 已支付 | 二次弹窗确认 | — | 已到账（balance += N） |
| 已支付 | 二次弹窗取消 | — | 已支付未到账（人工介入） |
| 待支付 | 支付失败 | — | 失败 |
| 已到账 | — | — | 流水写入完成 |

#### 3.4.2 后台积分操作状态机

| 起始 | 动作 | 前置 | 目标 |
|------|------|------|------|
| 配置变更 | 保存初始用户积分 | — | 新增配置快照（valid_from = 当前时刻） |
| 用户注册 | 系统按 valid_from ≤ 注册日 的最新配置发放 | — | 余额 = 配置值 |
| 运营调整积分 | 赠送 | — | 余额 +N + 流水写入 |
| 运营调整积分 | 扣除（输入 ≤ 余额） | — | 余额 -N + 流水写入 |
| 运营调整积分 | 扣除（输入 > 余额） | 二次确认通过 | 积分为 0 + 流水写入 |
| 用户生图 | 调用 Coze 前 | 积分 ≥ 1 | 余额 -cost + 流水写入（来源：生图消耗） |
| 用户生图 | 调用 Coze 前 | 积分为 0 | 拦截（前端+后端双校验） |

---

## 四、功能需求详情

### 4.1 C 端 · 我的作品（V0.4 增量改造）

**需求影响范围**：在 V0.4 既有页面顶部卡上新增「购买积分」「积分明细」两枚按钮，账户卡片保留两列展示。   **涉及终端明细**：绿韵家 App C 端 H5。

#### 4.1.1 页面原型与交互示意

参考 `lvyunjia-ai-generation-proto.html` 中 `pageHistory`，改造积分账户卡片区域（截图参考：用户提供的「我的作品」手机界面）。

#### 4.1.2 详细逻辑规则

**界面与展示规则**
- 顶部导航栏：左「‹」返回，中间标题「我的作品」，右侧「创作」按钮 → 进入 AI 生图页（沿用 V0.4）；
- 积分账户卡片（改造）：
  - 第一行文案「积分账户」（⚠️ V0.4 文档为「积分账户」，截图也为「积分账户」，本期保留不改名）；
  - 数字列（沿用 V0.4）：左列「本月已用」+ 数值；右列「我的作品」+ 数值；
  - **按钮行**（本期新增）：左「购买积分」主按钮（绿底白字）+ 右「积分明细」次按钮（白底灰描边）；
- 生成记录卡片：沿用 V0.4，不改动；
- 空态：沿用 V0.4 不改动。

**业务逻辑与边界条件**
- 「购买积分」点击 → 跳转 `pageRecharge`（积分购买页，4.2）；
- 「积分明细」点击 → 跳转 `pageCreditDetail`（积分明细页，4.3）；
- 「立即创作」/「创作」点击 → 沿用 V0.4，前置校验余额（参考 4.4）；
- 未登录用户：所有按钮点击 → 跳转登录页，登录后回到原页；
- 积分为 0 时：「购买积分」按钮可点击；「立即创作」/「创作」按 V0.4 走提示积分不足拦截。

#### 4.1.3 字段表

| 字段 | 数据来源 | 说明 |
|------|----------|------|
| 剩余积分 | ai_credit_account.balance | ⚠️ V0.4 仅展示"本月已用/我的作品数"；**本期是否增加"剩余积分"展示列为待确认项 1** |
| 本月已用 | ai_credit_log：SUM(amount) WHERE change = '-' AND created_at 本月 | V0.4 已展示 |
| 累计充值 | ai_credit_log：SUM(amount) WHERE source = '购买' | 本期新增（仅在积分明细页展示） |
| 我的作品 | ai_generation_log WHERE user_id = 当前 AND status = success 计数 | V0.4 已展示 |

#### 4.1.4 按钮权限表

| 按钮 | 权限 | 前置条件 |
|------|------|----------|
| 购买积分 | 已登录 | 任意时刻可点击 |
| 积分明细 | 已登录 | 任意时刻可点击 |
| 立即创作 / 创作 | 已登录 | 积分 ≥ 1（前端校验） |

---

### 4.2 C 端 · 积分购买（本期新增）

**需求影响范围**：C 端新增页面；用户从我的作品卡跳转进入，购买后到账；本期不做退款。   **涉及终端明细**：绿韵家 App C 端 H5。

#### 4.2.1 页面原型与交互示意

C 端原型 `lvyunjia-ai-generation-proto.html` 中新增 `pageRecharge`（独立页）。

#### 4.2.2 详细逻辑规则

**界面与展示规则**
- 顶部导航栏：左「‹」返回我的作品，中间标题「积分购买」；
- 套餐选择区：卡片列表，每张卡片展示：
  - 积分值（如「30 积分」「100 积分」「300 积分」「1000 积分」）；
  - **价格动态计算**：`套餐积分数 × 当前有效 price_per_credit`（如定价 ¥0.10/积分 → 100 积分 = ¥10.0）；
  - 标签「推荐」「超值」「特惠」按运营配置可显示；
  - **新人首单额外展示**：「首单再送 X 积分」（X = 套餐积分数），仅 `is_first_purchase = true` 时显示；
  - 单选：默认未选中，套餐卡片单选互斥；
- 底部固定「立即购买」按钮（绿底白字）：
  - 未选套餐时置灰，提示「请先选择套餐」；
  - 已选套餐时显示动态价格（如「立即购买 ¥10.0」）；
- 页面底部文案（⚠️ 法务文案待确认项 2）：
  - **硬性文案「购买后不支持退款」** 用显眼样式（如红色加粗）独立一行；
  - 次行「购买后积分永久有效，可在积分明细查看」；
- 二次弹窗（关键）：支付成功后弹出，全屏居中弹窗：
  - 标题「购买后不支持退款」；
  - 内容文案：
    - 普通用户：「购买 X 积分 · 支付 ¥Y · 购买后不支持退款」
    - 新人首单：「购买 X 积分 · 支付 ¥Y · 🎁 新人首单额外赠送 X 积分（实到账 2X 积分）· 购买后不支持退款」
  - 两枚按钮：「确认到账」（主）+「取消」（次）；
  - **必须用户点击「确认到账」才完成入账**；否则订单状态置为"已支付未到账"。

**业务逻辑与边界条件**
- **定价模型**（V0.5 核心）：
  | 参数 | 说明 | 示例 |
  |------|------|------|
  | `price_per_credit` | 后台「积分定价管理」配置，1 积分 = 多少元 | ¥0.10 / 积分 |
  | 套餐积分数 | 前端固定档位（运营可后续改为后台配置） | 30 / 100 / 300 / 1000 |
  | 套餐价格 | `积分数 × price_per_credit`（动态计算） | 100 × 0.10 = ¥10.0 |
  | 新人首单赠送 | 首次购买时 `bonus_credits = paid_credits` | 买 100 送 100 |
- 套餐字典（⚠️ 待确认项 3 - 运营给出最终值）：
  | 套餐 ID | 名称 | 积分值 | 标签 | 状态 |
  |---------|------|--------|------|------|
  | pkg_30 | 30 积分 | 30 | — | 默认展示 |
  | pkg_100 | 100 积分 | 100 | 推荐 | 默认展示 |
  | pkg_300 | 300 积分 | 300 | 超值 | 默认展示 |
  | pkg_1000 | 1000 积分 | 1000 | 特惠 | 默认展示 |
  - 价格不在此表定义，由 `price_per_credit` 动态计算；
  - 运营改 `price_per_credit` 即全站生效，无需逐个改套餐；
- **新人首单优惠逻辑**：
  1. 后端判断 `is_first_purchase`（查询 ai_purchase_order 表该用户是否已有 status=已到账 的订单）；
  2. 首单 → 二次弹窗展示赠送提示 → 确认后 `balance += paid_credits + bonus_credits`（bonus = paid_credits）；
  3. 非首单 → 正常流程，无赠送；
  4. 首单标志位：订单表 `is_first_order` 字段标记，仅触发一次；
- 支付流程：
  1. 用户选套餐 → 点立即购买 → 调 `/credit/purchase` 创建订单（status=待支付，快照当时的 `price_per_credit` 到 `pricing_snapshot_id`）；
  2. 跳转支付 SDK（微信/支付宝/银联，具体支付渠道由后端封装，本期前端只接一个统一入口）；
  3. 支付回调 → status=已支付；
  4. **强制弹窗"购买后不支持退款"二次确认** → 用户点确认 → status=已到账 → balance += paid_credits (+ bonus_credits if 首单) → 写入积分流水（source=购买）；
- 退款流程：**本期不实现**。如用户点"取消"二次确认，订单状态置为"已支付未到账"，后台运营手工处理（人工退款走线下渠道）；
- 网络异常：
  - 下单失败：toast「下单失败，请重试」；
  - 支付回调超时：订单状态置为"待确认"，下次进入购买页时弹窗提示「存在未完成订单，是否继续处理」；
- 未登录用户：自动跳转登录页，登录后回到套餐选择页（套餐选择不保留）。

#### 4.2.3 字段表（购买订单）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| order_id | String | 是 | 订单号（生成规则沿用现有订单号生成器） |
| user_id | String | 是 | 当前用户 |
| pkg_id | String | 是 | 套餐 ID |
| pkg_value | Integer | 是 | 套餐积分值 |
| pay_amount | Decimal | 是 | 实付金额 |
| status | Enum | 是 | 待支付 / 已支付 / 已支付未到账 / 已到账 / 失败 / 已取消 |
| created_at | DateTime | 是 | 创建时间 |
| pay_at | DateTime | 否 | 支付回调时间 |
| confirm_at | DateTime | 否 | 二次确认时间 |
| refund_at | DateTime | 否 | 退款时间（本期始终为 null） |

#### 4.2.4 验收标准

- **AC1**：Given 用户从我的作品点击「购买积分」，When 进入购买页，Then 默认无套餐选中，「立即购买」置灰。
- **AC2**：Given 用户选择「30 次套餐」，When 点击「立即购买」，Then 跳转支付 SDK 并创建状态为"待支付"的订单。
- **AC3**：Given 支付回调成功，When 弹窗「支付成功/购买后不支持退款」出现，Then 用户必须点击「确认到账」才入账。
- **AC4**：Given 用户在二次弹窗点击「取消」，Then 订单状态置为"已支付未到账"，后台运营收到通知。
- **AC5**：Given 用户点击底部文案"购买后不支持退款"，Then 显示完整的退款政策说明（弹窗或跳转协议页，⚠️ 待确认项 2）。

---

### 4.3 C 端 · 积分明细（本期新增）

**需求影响范围**：C 端新增页面；用户可随时查看积分账户与流水，顶部"充值"按钮复用 4.2。   **涉及终端明细**：绿韵家 App C 端 H5。

#### 4.3.1 页面原型与交互示意

C 端原型 `lvyunjia-ai-generation-proto.html` 中新增 `pageCreditDetail`（独立页）。视觉参考用户提供的「积分明细」手机界面。

#### 4.3.2 详细逻辑规则

**界面与展示规则**
- 顶部导航栏：左「‹」返回我的作品，中间标题「积分明细」，**右上角「充值」按钮**（点击跳 4.2）；
- 积分账户卡片：
  - 三列展示：**剩余积分 / 本月已用 / 累计充值**（截图为准）；
  - 数据来源同 4.1.3 字段表；
- Tab 切换（截图为准）：
  - 「全部」（默认）：展示所有流水；
  - 「消费」（change = '-'）：仅展示积分扣除流水；
  - 「充值」（source = 购买 / 后台赠送）：仅展示积分增加流水；
- 流水列表项：
  - 图标（生图=画板、购买=💰、后台赠送=礼盒、后台扣除=✂️）；
  - 描述文案（如「生成图片 · 消耗 1 积分」「购买 30 次套餐 · 到账 30 积分」「新用户赠送 · 到账 3 积分」）；
  - 时间（YYYY-MM-DD HH:mm）；
  - 数值（红色 -N 或 绿色 +N）；
  - 底部文案「— 已显示全部记录 —」（无更多时显示）；
- 空态：当前 Tab 无流水时显示「暂无相关记录」空态插图与文案。

**业务逻辑与边界条件**
- 「充值」按钮 → 跳转 `pageRecharge`（4.2）；
- 列表分页：本期实现"全量展示"+「已显示全部记录」文案，**不做分页加载**（⚠️ 待确认项 4 - 数据量超过 100 条是否需要分页）；
- 「本月已用」按自然月统计，月初自动重置；
- 「累计充值」= 历次购买 + 后台赠送的总和（不含后台扣除）；
- 时间倒序展示，最新一条在最上。

#### 4.3.3 字段表（流水）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| log_id | String | 是 | 流水 ID |
| user_id | String | 是 | 用户 |
| change | Enum | 是 | +/- （加 / 减） |
| amount | Integer | 是 | 积分变化值 |
| source | Enum | 是 | 购买 / 后台赠送 / 后台扣除 / 生图消耗 / 系统赠送（新用户初始值） |
| reason | String | 否 | 描述文案（运营填的备注） |
| balance_after | Integer | 是 | 变化后余额 |
| created_at | DateTime | 是 | 时间 |

#### 4.3.4 验收标准

- **AC1**：Given 用户从我的作品点击「积分明细」，When 进入明细页，Then 默认显示「全部」Tab + 完整流水列表。
- **AC2**：Given 用户点击「消费」Tab，Then 仅展示 change='-' 的流水（生图消耗 + 后台扣除）。
- **AC3**：Given 用户点击「充值」Tab，Then 仅展示 source ∈ {购买, 后台赠送} 的流水。
- **AC4**：Given 用户无任何流水，Then 显示「暂无相关记录」空态。

---

### 4.4 C 端 · AI 生图（V0.4 增量 - 余额不足拦截）

**需求影响范围**：在 V0.4 AI 生图页加前置校验 + 余额不足拦截弹窗。   **涉及终端明细**：绿韵家 App C 端 H5。

#### 4.4.1 详细逻辑规则

**消耗提示（本期新增，3 处联动后台配置）**

AI 生图页在**三处**展示本次生成的积分消耗，数值全部取后台「生图成本配置」（§4.9）当前生效值 `cost_per_image`，**前端不硬编码**：

| # | 位置 | 文案模板 | 数据来源 |
|---|------|----------|----------|
| 1 | 输入区下方提示行 | 本次生成将消耗 **N** 个积分 ｜ 积分管理 › | `GET /credit/image-cost-config`（取 `valid_from ≤ 当前` 最新一条的 `cost_per_image`） |
| 2 | 底部固定栏 | 预计消耗 **N** 积分 | 同上 |
| 3 | 积分不足弹窗 | 本次生成需要 **N** 个积分，你的剩余积分为 X | 同上 |

- 三处数值必须**同源同值**，运营在后台改一次成本配置，C 端三处同步变化；
- 接口返回失败/超时时，三处显示兜底值 **1** 并在控制台告警，不阻塞生成（⚠️ 待确认项 17：兜底值是否取 1）。

**前置条件**
- 用户点击「立即生成」/「创作」按钮；
- 后端校验积分 ≥ `cost_per_image`；
- 校验通过 → 调用 Coze 生图；
- 校验失败 → 走积分不足流程。

**积分不足流程**
- 弹窗：居中弹窗，标题「积分不足」，内容「本次生成需要 **N** 个积分，你的剩余积分为 0」（N = 成本配置值）；
- 两枚按钮：
  - 「立即充值」（主，绿底）→ 跳转 `pageRecharge`（4.2），保留生图页的提示词；
  - 「取消」（次）→ 关闭弹窗回到生图页，提示词保留；
- 弹窗出现时不直接清空提示词，用户充值完成后回到生图页可继续创作。

**余额充足流程**
- 沿用 V0.4 不改动：调 Coze → 扣 cost → 写入流水（source=生图消耗）→ 弹结果；
- 扣减时机：Coze 回调成功后扣减，失败不扣；
- 成本取 `ai_image_cost_config` 当前生效值（按 valid_from 最新一条）。

#### 4.4.2 字段表（生图消耗流水，关联 ai_credit_log）

| 字段 | 类型 | 说明 |
|------|------|------|
| log_id | String | 流水 ID（与 ai_credit_log 共用） |
| source | Enum | 生图消耗 |
| amount | Integer | 当次消耗积分 |
| generation_id | String | 关联 ai_generation_log.id（V0.4 已有） |
| credit_cost | Integer | 本期新增，关联成本配置（冗余存储便于追溯） |

#### 4.4.3 验收标准

- **AC1**：Given 用户积分为 0，When 点击「立即生成」，Then 弹窗「积分不足」+ 两枚按钮。
- **AC2**：Given 用户在余额不足弹窗点击「立即充值」，When 跳转购买页完成购买并确认到账，Then 回到生图页提示词仍保留。
- **AC3**：Given 用户积分 ≥ 1，When 点击「立即生成」，Then 正常调 Coze 生图并扣减积分。
- **AC4**：Given Coze 回调失败，When 用户看结果，Then 提示「生成失败，请重试」且**不扣积分**。

---

### 4.5 后台 · 会员管理列表（截图改造）

**需求影响范围**：截图所在后台页加列/筛选项/操作按钮，原有列与操作保持不变。   **涉及终端明细**：绿韵家管理系统 - 会员管理。

#### 4.5.1 页面原型与交互示意

后台原型 `lvyunjia-admin-credit-proto.html`（本期新建）中 `pageMemberList`（独立页）。视觉参考用户提供的「会员中心列表」后台截图。

#### 4.5.2 详细逻辑规则

**现有列（保留）**
- 会员编号、会员信息（头像+昵称）、手机号、会员等级、当前会员等级、是否已订阅、实付薪、执行数、推荐人、标签、实名认证状态、限制折扣、是否禁用、注册时间、操作

**新增列：积分余额（本期）**
- 位置：放在「当前会员等级」之后、「是否已订阅」之前；
- 展示：整数数字（如「12」「0」），为0时标红；
- 数据来源：`ai_credit_account.balance`（联表查询）；
- 点击单元格 → 跳转该用户的积分明细页（仅查看，不可调整；调整为「操作」列入口）。

**新增筛选项：按积分余额筛选（本期）**
- 位置：搜索区；
- 类型：单选下拉 + 区间输入双形态：
  - 单选：`全部`（默认）/ `= 0` / `> 0` / `< 0`（⚠️ 待确认项 5 - 是否支持 `< 0`，理论上余额不应为负）；
  - 区间：`区间最小值 ~ 区间最大值`（两个数字输入框，可选）；
- 与既有筛选项共存，叠加生效。

**新增操作按钮：调整积分（本期）**
- 位置：每行「操作」列的现有按钮组中；
- 顺序：⚠️ 待确认项 6 - 当前已有「查看/修改手机号/修改推荐人/重置密码/调整级别/调整等级/设置执行/删除/修改执行」9 个按钮，新增「调整积分」放第几位 / 是否折叠为「更多」下拉；
- 点击 → 弹窗 `adjustCreditModal`（4.6）。

#### 4.5.3 字段表

| 字段 | 数据来源 | 说明 |
|------|----------|------|
| 积分余额 | ai_credit_account.balance | 联表 LEFT JOIN，无账户记录时展示 0 |

#### 4.5.4 验收标准

- **AC1**：Given 运营打开会员管理列表，When 加载完成，Then 每行新增「积分余额」列展示用户积分，为 0 时标红。
- **AC2**：Given 运营选择筛选项 `= 0`，When 刷新列表，Then 仅展示积分为 0 的用户。
- **AC3**：Given 运营在区间输入 `10~50`，When 刷新列表，Then 仅展示余额在 [10, 50] 的用户。
- **AC4**：Given 运营点击某行的「调整积分」，When 弹窗出现，Then 显示当前用户信息 + 两个 Tab（赠送/扣除）。

---

### 4.6 后台 · 调整积分弹窗（本期新增）

**需求影响范围**：会员管理列表每行的「调整积分」按钮触发。   **涉及终端明细**：绿韵家管理系统。

#### 4.6.1 页面原型与交互示意

后台原型 `lvyunjia-admin-credit-proto.html` 中 `adjustCreditModal`。

#### 4.6.2 详细逻辑规则

**界面与展示规则**
- 弹窗：居中模态，标题「调整积分 - {用户昵称}」；
- 顶部信息：当前积分（醒目展示）；
- Tab 切换：
  - 「赠送」（默认）：表单 = 数值 + 备注；
  - 「扣除」：表单 = 数值 + 备注 + 二次确认（输入值 > 余额时）；
- 数值输入：必填，整数，1~99999（⚠️ 待确认项 7 - 单次上限是否调整）；
- 备注输入：选填，文本，最多 200 字；
- 底部按钮：「确定」（主）+「取消」；
- 操作完成后 toast「调整成功」并刷新当前积分。

**业务逻辑与边界条件**
- 赠送 → 直接 `balance += N` + 写入 ai_credit_log（source=后台赠送，operator=当前运营）；
- 扣除（输入 ≤ 余额）→ `balance -= N` + 写入 ai_credit_log（source=后台扣除，operator=当前运营）；
- 扣除（输入 > 余额）→ 二次确认弹窗「将扣除至 0，确认？」→ 确认后 `balance = 0` + 写入流水 → 取消则关闭弹窗回到原页；
- 扣除不可为负数（输入 < 1 时校验失败，提示「请输入 ≥ 1 的整数」）；
- 操作日志写入后台操作日志表（沿用既有），便于审计。

#### 4.6.3 字段表（操作日志，ai_credit_log）

| 字段 | 类型 | 说明 |
|------|------|------|
| operator_id | String | 运营 ID |
| operator_name | String | 运营姓名（便于展示） |
| source | Enum | 后台赠送 / 后台扣除 |
| amount | Integer | 调整值 |
| reason | String | 备注 |
| ip | String | 操作 IP（沿用） |
| created_at | DateTime | 操作时间 |

#### 4.6.4 验收标准

- **AC1**：Given 运营在弹窗输入赠送 30，When 提交，Then 余额 +30 + 流水写入 + 操作日志写入。
- **AC2**：Given 用户积分 10，运营输入扣除 30，When 提交，Then 二次确认弹窗出现。
- **AC3**：Given 二次确认通过，When 提交，Then 积分为 0 + 流水写入 amount=10（实际扣除值）。
- **AC4**：Given 二次确认取消，When 操作完成，Then 余额不变，弹窗关闭。

---

### 4.7 后台 · 用户生成日志（本期新增）

**需求影响范围**：后台新增独立页，支持按用户/时间/内容类型/状态筛选。   **涉及终端明细**：绿韵家管理系统。

#### 4.7.1 页面原型与交互示意

后台原型 `lvyunjia-admin-credit-proto.html` 中 `pageGenerationLog`。

#### 4.7.2 详细逻辑规则

**界面与展示规则**
- 顶部导航栏：左「‹」返回会员管理，中间标题「用户生成日志」；
- 搜索区：用户（输入框 + 联想选择）、内容类型（下拉：全部/生图/素材下载）、状态（下拉：全部/成功/失败/进行中）、时间范围（日期区间）；
- 列表列：用户（头像+昵称）、生成时间、内容类型、提示词（单行截断）、消耗积分、结果 URL（点击预览）、状态、操作（查看详情/复制 URL/申诉）；
- 分页：每页 20 条，标准翻页器；
- 导出：右上角「导出」按钮，导出当前筛选项的 CSV（⚠️ 待确认项 8 - 是否本期实现）。

**业务逻辑与边界条件**
- 用户联想输入：从 `users` 表拉取（⚠️ 待确认项 9 - 是否本期实现联想，还是仅按 user_id 精确匹配）；
- 时间范围：默认近 7 天，最大可选近 90 天（⚠️ 待确认项 10 - 数据保留时长，超期是否可查询历史）；
- 结果 URL：仅展示，不下载（下载走 V0.4 既有能力）；
- 列表点击「查看详情」→ 弹窗：完整提示词 + 完整结果 URL + 关联流水；
- 列表点击「申诉」→ 弹窗：输入申诉理由 + 提交（⚠️ V0.5 是否实现申诉流程待确认项 11）。

#### 4.7.3 字段表（ai_generation_log）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 主键 |
| user_id | String | 用户 |
| content_type | Enum | image（V0.4 已有；视频 V0.7 再加） |
| prompt | Text | 完整提示词 |
| result_url | String | 结果 URL |
| status | Enum | 进行中 / 成功 / 失败 |
| credit_cost | Integer | **V0.5 新增**：本次消耗积分 |
| generation_time | Integer | 生成耗时（秒） |
| error_msg | String | 失败原因（成功时为 null） |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

#### 4.7.4 验收标准

- **AC1**：Given 运营打开用户生成日志，When 加载完成，Then 默认展示近 7 天全部用户的全部类型记录。
- **AC2**：Given 运营选择状态「失败」，When 刷新列表，Then 仅展示 status = 失败的记录。
- **AC3**：Given 运营点击「查看详情」，When 弹窗出现，Then 展示完整提示词 + 完整 URL + 关联流水。

---

### 4.8 后台 · 初始用户积分配置（本期新增）

**需求影响范围**：后台新增独立页；运营可设置新用户注册时发放的初始积分，带生效日期 + 历史快照。   **涉及终端明细**：绿韵家管理系统。

#### 4.8.1 页面原型与交互示意

后台原型 `lvyunjia-admin-credit-proto.html` 中 `pageInitConfig`。

#### 4.8.2 详细逻辑规则

**界面与展示规则**
- 顶部导航栏：左「‹」返回系统设置，中间标题「初始用户积分配置」；
- 顶部当前生效卡片：展示当前生效配置（积分值 + 生效日期 + 设置人 + 备注）；
- 设置表单：积分值（整数输入）+ 生效日期（日期选择器，默认今日）+ 备注（文本，选填，最多 200 字）；
- 「保存」按钮：保存后立即生效为新的一条历史快照；
- 历史快照列表：倒序展示，每条含积分值 + 生效日期 + 设置人 + 备注；
- 「作废」操作（仅历史快照）：把历史快照置为无效（⚠️ 待确认项 12 - 是否支持作废，作废后行为如何）。

**业务逻辑与边界条件**
- 生效机制：**新用户注册时**，系统按 `valid_from ≤ 注册日` 的最新一条配置发放；
- **已注册的老用户不动**（不追溯、不冻结、不作废其初始积分）；
- 生效日期 = 当前时刻（用户设置时即生效，不允许设置过去的日期作为生效日期，避免歧义）；
- 单次保存自动新增一条快照，旧快照仍可查；
- 作废（若实现）：作废后该条配置不再被任何注册用户匹配，最新未作废的快照继续生效；作废前已注册用户的积分不受影响；
- 校验：积分值 ≥ 0 且 ≤ ⚠️ 待确认项 13（初始积分上限）。

#### 4.8.3 字段表（ai_credit_init_config）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 主键 |
| value | Integer | 是 | 初始积分值 |
| valid_from | DateTime | 是 | 生效日期（用户设置时 = 当前时刻） |
| operator_id | String | 是 | 设置人 |
| operator_name | String | 是 | 设置人姓名 |
| remark | String | 否 | 备注 |
| status | Enum | 是 | 生效 / 已作废 |
| created_at | DateTime | 是 | 创建时间 |

#### 4.8.4 验收标准

- **AC1**：Given 运营保存新配置积分 = 50，When 保存，Then 数据库新增一条快照（valid_from = 当前时刻），旧快照仍展示在历史列表。
- **AC2**：Given 新用户在保存后注册，Then 自动获得 50 积分。
- **AC3**：Given 旧用户在保存前已注册，Then 其初始积分保持不变（不追溯、不冻结）。
- **AC4**：Given 历史快照被作废，Then 该条不再被任何注册匹配，最新未作废快照继续生效。

---

### 4.9 后台 · 生图成本配置（本期新增）

**需求影响范围**：后台新增独立页；运营可设置每张图扣多少积分，带生效日期 + 历史快照。   **涉及终端明细**：绿韵家管理系统。

#### 4.9.1 页面原型与交互示意

后台原型 `lvyunjia-admin-credit-proto.html` 中 `pageCostConfig`。

#### 4.9.2 详细逻辑规则

**界面与展示规则**
- 顶部导航栏：左「‹」返回系统设置，中间标题「生图成本配置」；
- 顶部当前生效卡片：展示当前生效配置（每张图扣 N 积分 + 生效日期 + 设置人 + 备注）；
- 设置表单：每张图扣多少积分（整数输入）+ 生效日期 + 备注；
- 「保存」按钮；
- 历史快照列表（同 §4.8）。

**业务逻辑与边界条件**
- 生效机制：用户调用生图时，按 `valid_from ≤ 调用时刻` 的最新一条配置计算扣减；
- 已消耗的积分不回溯（不重算历史生成的成本）；
- V0.7 视频上线时复用本表结构（新增 content_type 字段区分图/视频）；
- 校验：成本 ≥ 1 且 ≤ ⚠️ 待确认项 14（单图成本上限）。

#### 4.9.3 字段表（ai_image_cost_config）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | String | 是 | 主键 |
| cost_per_image | Integer | 是 | 每张图扣多少积分 |
| valid_from | DateTime | 是 | 生效日期 |
| operator_id | String | 是 | 设置人 |
| operator_name | String | 是 | 设置人姓名 |
| remark | String | 否 | 备注 |
| status | Enum | 是 | 生效 / 已作废 |
| content_type | Enum | 否 | image（V0.5 固定为 image；V0.7 视频扩展） |
| created_at | DateTime | 是 | 创建时间 |

#### 4.9.4 验收标准

- **AC1**：Given 运营保存成本 = 2，When 保存，Then 数据库新增一条快照，旧快照仍展示在历史列表。
- **AC2**：Given 用户在保存后调用生图，Then 按成本 = 2 扣减积分。
- **AC3**：Given 旧成本 = 1，用户在保存前已生成图片，Then 历史生成仍按成本 = 1 入账（不追溯重算）。
- **AC4**：Given 历史快照被作废，Then 该条不再被任何生成匹配，最新未作废快照继续生效。

---

## 五、非功能性需求

### 5.1 性能
- 积分购买页加载 < 1.5s（套餐字典拉取）；
- 积分明细页流水加载 < 1.5s（默认全部展示，无分页）；
- 后台会员管理列表查询响应 < 2s（10 万级用户量下，联表查询需走 `ai_credit_account.user_id` 索引）；
- 后台生成日志查询响应 < 2s；
- 初始用户积分 / 生图成本配置保存响应 < 500ms。

### 5.2 安全
- 所有积分相关接口必须鉴权 + 签名（沿用现有接口签名机制）；
- 后台调整积分必须二次确认（扣除场景）；
- 操作日志保留 ≥ ⚠️ 待确认项 15（默认 1 年）；
- 敏感字段（用户积分、流水金额）不在前端日志中打印。

### 5.3 可用性
- 二次弹窗"购买后不支持退款"必须在 iOS Safari / Android WebView / 微信内置浏览器 均能正常弹出；
- 后台弹窗在 1920×1080 / 1366×768 / 1280×800 分辨率下均能完整展示；
- 移动端横竖屏切换不破坏页面布局。

### 5.4 兼容性
- C 端：iOS Safari 14+ / Android WebView 80+ / 微信内置浏览器最新版；
- 后台：Chrome 100+ / Edge 100+。

### 5.5 埋点需求

| 事件 ID | 名称 | 触发时机 | 字段 |
|---------|------|----------|------|
| credit_enter_purchase | 进入积分购买 | 用户点击购买积分 | source（入口：我的作品/余额不足弹窗） |
| credit_pkg_select | 选择套餐 | 用户选择套餐 | pkg_id |
| credit_purchase_submit | 提交购买 | 用户点击立即购买 | pkg_id、pay_amount |
| credit_purchase_pay_success | 支付成功 | 支付回调成功 | order_id |
| credit_purchase_confirm | 确认到账 | 用户点击确认到账 | order_id |
| credit_purchase_cancel | 取消到账 | 用户点击取消 | order_id |
| credit_enter_detail | 进入积分明细 | 用户点击积分明细 | source |
| credit_tab_switch | Tab 切换 | 用户切换 Tab | tab（全部/消费/充值） |
| generation_insufficient_balance | 余额不足拦截 | 弹窗余额不足 | user_id |
| admin_credit_adjust | 后台调整积分 | 运营提交调整 | operator_id、user_id、source（赠送/扣除）、amount |
| admin_init_config_save | 保存初始积分配置 | 运营保存 | operator_id、value |
| admin_cost_config_save | 保存生图成本配置 | 运营保存 | operator_id、cost |

---

## 六、验收标准

### 6.1 C 端
- **AC-1**：在我的作品页能正常看到「购买积分」「积分明细」按钮，点击均可跳转对应页；
- **AC-2**：在 AI 生图页积分为 0 时点击「立即生成」，能看到「积分不足」弹窗，点击「立即充值」跳转购买页且原提示词保留；
- **AC-3**：在积分购买页能正常选套餐、下单、支付，看到二次弹窗，点击「确认到账」后余额到账且积分明细有新流水；
- **AC-4**：在积分明细页能看到账户卡片三列（剩余/本月已用/累计充值），Tab 切换正常，流水列表倒序展示。

### 6.2 后台
- **AC-5**：在会员管理列表能看到新增的「积分余额」列、筛选项、操作按钮「调整积分」，均功能正常；
- **AC-6**：在调整积分弹窗能正常赠送和扣除（含扣除超额二次确认）；
- **AC-7**：在用户生成日志页能按用户/时间/类型/状态筛选，能查看详情；
- **AC-8**：在初始用户积分配置页能保存新配置并看到历史快照，能保证老用户积分不变；
- **AC-9**：在生图成本配置页能保存新成本并看到历史快照，能保证已生成图片不重算。

### 6.3 跨端一致性
- **AC-10**：C 端生图扣减的金额 = 积分流水记录的金额 = 后台生成日志消耗积分的金额，三端一致；
- **AC-11**：后台调整积分后，C 端刷新即看到最新余额（缓存 TTL ≤ ⚠️ 待确认项 16）。

---

## 七、关键边界设计检查清单

| 检查项 | 是否满足 | 说明 |
|--------|----------|------|
| 不超出本期范围 | ✅ | 仅做积分管理 + 截图改造；视频、广场、做同款不在本期 |
| 不臆造数据 | ✅ | 待确认项 16 处（详见 §八），均用「⚠️ 待确认」占位或反问 |
| §四 按页面拆解 | ✅ | C 端 4 页（4.1~4.4）+ 后台 5 页（4.5~4.9） |
| 字段表完整 | ✅ | 每页均含字段表 |
| 状态机完整 | ✅ | 购买订单状态机 + 后台积分操作状态机 |
| 流程图完整 | ✅ | 4 张流程图（购买/不足拦截/后台积分管理/成本生效） |
| 业务边界明确 | ✅ | 退款、网络异常、未登录、运营误操作等均有兜底 |
| 不跨端口做冗余 | ✅ | 后台与 C 端职责明确，运营场景只后台、C 端场景只前端 |

---

## 八、待确认项汇总

| # | 待确认 | 责任人 | 影响范围 |
|---|--------|--------|----------|
| 1 | 我的作品积分账户卡片是否增加「剩余积分」展示列（V0.4 仅展示本月已用+我的作品数） | @产品 | §4.1 |
| 2 | 购买页底部"购买后不支持退款"文案的最终版（是否需要协议页跳转） | @法务 | §4.2 |
| 3 | 套餐字典最终值（名称/积分/价格/标签） | @运营 | §4.2 |
| 4 | 积分明细流水是否需要分页（超过 100 条） | @产品 | §4.3 |
| 5 | 是否支持筛选项 `< 0`（理论上余额不应为负） | @产品 | §4.5 |
| 6 | 会员管理「操作」列新增「调整积分」按钮的放置位置（直接展示 / 折叠为「更多」下拉） | @产品 | §4.5 |
| 7 | 调整积分单次操作上限（V0.4 业务定义：单次操作 ≤ 99,999） | @产品 | §4.6 |
| 8 | 用户生成日志是否本期实现 CSV 导出 | @产品 | §4.7 |
| 9 | 用户生成日志用户搜索是否实现联想 | @研发 | §4.7 |
| 10 | 用户生成日志数据保留时长 + 超过保留时长是否可查询 | @研发 | §4.7 |
| 11 | 用户生成日志「申诉」流程本期是否实现 | @产品 | §4.7 |
| 12 | 初始用户积分配置历史快照是否支持「作废」操作 | @产品 | §4.8 |
| 13 | 初始积分上限值 | @运营 | §4.8 |
| 14 | 单图成本上限值 | @运营 | §4.9 |
| 15 | 后台操作日志保留时长 | @研发 | §5.2 |
| 16 | C 端积分相关字段缓存 TTL（购买成功后多久 C 端刷新可见） | @研发 | §6.3 |
| 17 | AI 生图页三处消耗提示的兜底值（后台成本配置接口失败时取多少，原型暂用 1） | @产品 / @研发 | §4.4.1 |
| 18 | 套餐档位最终值（原型暂用 30/100/300/1000 积分，价 ¥9.9/¥29.9/¥79.9/¥239.9） | @运营 | §4.2 |
| 19 | V0.7 视频成本值（每视频扣多少积分） | @运营 | §9.2 |
| 20 | V0.8 数字人分身第三方服务商选型（火山 / 腾讯智影 / 硅基智能）与计费模型 | @产品 / @商务 | §9.2 |
| 21 | V0.9 风控律师是否为大模型对话，是否需接外部法务 API，合规要求 | @产品 / @法务 | §9.2 |

---

## 九、AI 工具演进规划

> 本章定义除「图生图」外其余 AI 工具的**推进节奏**与**各版本完成度边界**，供后续按时间排期。

### 9.1 工具全景与本期状态

| 工具 | 定位 | V0.4 状态 | V0.5（本期） | 后续版本 |
|------|------|-----------|--------------|----------|
| **图片生成** | AI 生图（核心） | ✅ 已交付 | 🔴 商业化改造（积分体系 + 消耗提示） | 持续优化 |
| **素材库** | 营销素材下载 | ✅ 已交付（仅列表/预览/下载） | ⏸ 本期不动 | V0.6 二期增强 |
| **生成视频** | AI 生视频 | ❌ V0.2 评审砍掉 | ⏸ 本期不做 | **V0.7（11 月）** |
| **数字人分身** | 数字人口播 | ⏸ 仅 toast 占位 | ⏸ 本期保持 toast | **V0.8（12 月）** |
| **风控律师** | 法务咨询 | ⏸ 仅 toast 占位 | ⏸ 本期保持 toast | **V0.9（次年 Q1，独立 spec）** |

### 9.2 版本推进表

| 版本 | 时间 | 工具 | 完成度定义（做到什么程度算完） | 依赖 |
|------|------|------|--------------------------------|------|
| **V0.5** | 2026-09 | 图生图商业化 | C 端能买积分、能用积分生图、三处消耗提示联动后台配置；后台能查/赠/扣每人积分、能查生成日志、能配初始值与生图成本。**不做**：视频、退款、广场 | V0.4 |
| **V0.6** | 2026-10 | 素材库二期 | 后台能上传/编辑/分类/上下架/统计；C 端能搜索、按类型筛选、排序。目标：从"能看能下"升级为"能运营 + 能搜" | V0.4 列表页 |
| **V0.7** | 2026-11 | 生成视频 | 复用 V0.5 积分账户（同一套 `ai_credit`），新增视频成本配置项（结构与 §4.9 对称）；C 端视频生成页 + 结果页 + 生成记录 | **强依赖 V0.5** |
| **V0.8** | 2026-12 | 数字人分身 | MVP：文本 → 静态数字人 + 语音合成 → 输出 MP4；默认 3~5 个形象可选。**不做**：3D、动作、实时互动、换脸 | V0.5 + 第三方 API 选型 |
| **V0.9** | 次年 Q1 | 风控律师 | 独立业务，**需先单独出 spec 再排期**；不在 V0.5~V0.8 主链路内 | 独立评估 |

### 9.3 依赖关系

```
V0.5（图生图商业化 · 积分账户基础设施）
  ├─→ V0.7（生成视频）──→ V0.8（数字人分身）
  │        ↑ 必须复用 V0.5 的积分账户与成本配置结构
  └─→ V0.6（素材库二期）  可与 V0.5 并行，但需不同人力

V0.9（风控律师）  完全独立，不走上述链路
```

**关键约束**
- **V0.5 必须先于 V0.7**：视频要复用积分账户，V0.5 的 `ai_credit_account` / `ai_credit_log` / 成本配置表是 V0.7 的前置基础设施。
- **成本配置表预留扩展**：§4.9 的 `ai_image_cost_config` 已预留 `content_type` 字段（V0.5 固定 `image`），V0.7 视频上线时直接加 `video` 枚举即可，**不改表结构**。
- **V0.6 与 V0.5 可并行**：两者无数据依赖，但需评估人力是否允许双线推进。
- **V0.9 风控律师是独立业务**：不从 AI 工具主链路延伸，需重新做 spec（业务形态、合规、是否接外部 API 均未定）。

### 9.4 各版本不在范围内的能力（防止范围蔓延）

| 版本 | 明确不做 |
|------|----------|
| V0.5 | 视频生成、退款流程、创作广场、做同款、积分过期 |
| V0.6 | 素材审核流、素材版本管理、素材评论/评分 |
| V0.7 | 视频剪辑、视频模板、字幕合成、长视频（>60s） |
| V0.8 | 3D 数字人、动作捕捉、实时互动、换脸、自定义形象上传 |
| V0.9 | 待独立 spec 确定 |

---

## 十、附录

### 附录 A：与 V0.4 差异追溯表

| 旧（V0.4） | 新（V0.5） | 影响 |
|---|---|---|
| 积分账户卡片仅展示「本月已用」「我的作品数」 | 同上 + **新增「购买积分」「积分明细」按钮** | §4.1 改造 |
| 积分购买归后端（前端不实现） | C 端新增「积分购买」流程（4.2） | §4.2 新增 |
| 无「积分明细」C 端页 | C 端新增「积分明细」页（4.3） | §4.3 新增 |
| AI 生图无前置校验 | 加余额校验 + 余额不足拦截弹窗 | §4.4 改造 |
| 后台会员管理列表（截图） | 加列「积分余额」+ 筛选项 + 操作按钮「调整积分」 | §4.5 改造 |
| 无调整积分弹窗 | 新增「调整积分弹窗」（赠送/扣除） | §4.6 新增 |
| 无用户生成日志页 | 新增（4.7） | §4.7 新增 |
| 无初始用户积分配置 | 新增（4.8） | §4.8 新增 |
| 无生图成本配置 | 新增（4.9） | §4.9 新增 |

### 附录 B：术语表

| 术语 | 说明 |
|------|------|
| 积分 | 本期对外展示文案（与 V0.4 一致）；数据库 / 接口字段为 `ai_credit` |
| 流水 | 积分变化记录（来源：购买 / 后台赠送 / 后台扣除 / 生图消耗 / 系统赠送） |
| 二次弹窗 | 支付成功后必须用户点击「确认到账」才完成入账的全屏居中弹窗 |
| 生效日期（valid_from） | 配置项生效的时间点；注册用户按 ≤ 注册日 的最新值发放 |
| 历史快照 | 配置项每次保存均生成一条新记录，旧记录保留可查 |

### 附录 C：用户提供的截图清单

- `Clipboard_Screenshot.png`（2026-09-01 10:59）：绿韵家管理系统 - 会员中心列表页（后台基线样式参考）
- `Clipboard_Screenshot.png`（2026-09-01 14:46）：C 端「积分明细」「我的作品」两个手机界面（C 端基线样式参考）