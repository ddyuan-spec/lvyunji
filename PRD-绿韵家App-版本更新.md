# 绿韵家 App · 版本更新需求文档（PRD）

> **模块编号**：TASK-20260812-0004 ｜ **一级模块**：首页 ｜ **优先级**：P0 ｜ **端口**：App（Flutter）
> **技术栈**：Flutter ｜ **需求状态**：本期新增 ｜ **文档版本**：V1.0
> **关联文档**：`spec.md`、`kb/page-catalog.md`、`kb/function-inventory.md`
> **原型文件**：`prototypes/app-upgrade-v1.html`

---

## 一、文档基本信息

| 字段 | 内容 |
|------|------|
| 项目名称 | 绿韵家 App 重构（2026年8月期） |
| 需求模块 | 版本更新（App 升级） |
| 看板任务 | TASK-20260812-0004 |
| PM 负责人 | 袁中天 |
| 技术 Owner | （⚠️ 待确认 @宋健） |
| 测试负责人 | （⚠️ 待确认 @高旗·刘平·李哲） |
| 文档状态 | 需求确认稿（待评审） |

### 历史修订记录

| 版本 | 日期 | 修订内容 | 状态 |
|------|------|---------|------|
| V1.0 | 2026-08-13 | 初稿：检测更新 + 弹窗 + 强制/非强制更新 + 下载安装 | 待评审 |

---

## 二、项目背景与目标

### 2.1 业务背景

App 上线后需要持续迭代，版本更新能力确保用户能及时获取新功能、修复和性能优化。原小程序无此能力，App 端需从零建设版本检测与升级机制。

### 2.2 项目目标

| 目标 | 衡量指标 |
|------|---------|
| 强制更新覆盖率 | 关键版本 100% 拦截旧版本 |
| 非强制更新触达率 | 首页弹窗 + 设置入口双通道 |
| 更新成功率 | 用户点击更新后完成下载安装比例 |

### 2.3 业务要求

- 启动时静默检测版本，避免阻塞首屏。
- 区分**强制更新**与**非强制更新**：强制更新必须升级后才能使用；非强制更新允许跳过。
- 支持 iOS App Store 与 Android 应用市场/安装包双渠道跳转。
- 提供「我的 → 关于我们 → 检查更新」手动触发入口（本期不做设置页，仅预留接口）。

---

## 三、业务流程与架构

### 3.1 版本检测与弹窗流程

```
App 冷启动
    │
    ▼
请求版本检测接口（GET /api/app/version）
    │
    ├── 接口失败 → 静默失败，不影响使用
    │
    └── 返回最新版本信息
         │
         ├── 当前版本已是最新 → 无弹窗
         │
         └── 当前版本低于最新版
              │
              ├── 强制更新 → 展示强制更新弹窗，用户只能点击「立即更新」
              │
              └── 非强制更新 → 展示更新提示弹窗，可选「立即更新」/「暂不更新」
```

### 3.2 信息架构

| 页面/弹窗 | 入口 | 出口 |
|-----------|------|------|
| 强制更新弹窗 | App 启动检测到强制更新 | 跳转下载（iOS App Store / Android 市场或浏览器） |
| 非强制更新弹窗 | App 启动检测到非强制更新 | 跳转下载 或 关闭弹窗进入首页 |
| 更新进度提示 | 点击立即更新后 | 下载完成提示安装 |
| 设置页检查更新入口 | 我的 → 设置 → 关于我们（本期预留） | 触发版本检测 |

---

## 四、功能需求详情

### 4.0 需求影响范围

| 影响项 | 说明 |
|--------|------|
| 涉及终端 | App 端（Flutter），iOS + Android |
| 本期新增/改动 | 版本检测接口、更新弹窗、跳转下载 |
| 沿用现有功能 | 无 |
| 关联后台 | App 版本管理后台 |
| 不做项 | 应用内静默下载安装包（Android 本期暂不做热更新）、设置页入口（若设置页未在本期范围则预留） |

### 4.1 版本检测

#### 4.1.1 功能说明

App 启动后异步请求版本检测接口，对比当前安装版本号与服务器最新版本号，确定是否需要更新及更新类型。

#### 4.1.2 接口返回字段

| 字段名称 | 需求说明 | 备注 |
|---------|---------|------|
| latest_version | 最新版本号，如 "1.2.0" | — |
| min_version | 最低兼容版本号 | 低于此版本强制更新 |
| force_update | 是否强制更新 | true/false |
| update_title | 弹窗标题 | 如「发现新版本」 |
| update_content | 更新说明，支持多行文本 | — |
| download_url | Android 下载链接或跳转地址 | — |
| app_store_url | iOS App Store 地址 | — |

#### 4.1.3 业务逻辑与边界条件

| 步骤 | 操作 | 前置条件 | 异常/校验 | 提示文案 |
|------|------|---------|----------|---------|
| 1 | App 启动后 1s 内请求版本检测 | 网络可用 | 网络异常 | 静默失败，记录日志 |
| 2 | 解析版本号 | 接口返回成功 | 版本号格式异常 | 按未更新处理 |
| 3 | 判断更新类型 | current < min_version → 强制；current < latest → 非强制 | — | — |
| 4 | 弹窗展示 | 需要更新 | 用户上次已点「暂不更新」 | 非强制更新当天不再弹（本地记录） |

### 4.2 强制更新弹窗

#### 4.2.1 功能说明

当当前版本低于最低兼容版本时，展示全屏遮罩弹窗，用户无法关闭或跳过，必须点击更新。

#### 4.2.2 界面与展示规则

| 元素 | 展示规则 | 备注 |
|------|---------|------|
| 弹窗标题 | 居中，16px 加粗 | 取 update_title |
| 版本号 | 标题下方，14px | 如「V1.2.0」 |
| 更新说明 | 多行文本，12px | 取 update_content |
| 立即更新按钮 | 底部主按钮 | 点击跳转下载 |
| 关闭按钮 | 不展示 | 强制更新不可关闭 |

#### 4.2.3 业务逻辑与边界条件

| 步骤 | 操作 | 前置条件 | 异常/校验 | 提示文案 |
|------|------|---------|----------|---------|
| 1 | 展示强制弹窗 | current < min_version | — | — |
| 2 | 点击立即更新 | — | — | 跳转对应平台下载页 |
| 3 | 返回 App | 用户未安装新包 | — | 弹窗仍在，继续拦截 |

### 4.3 非强制更新弹窗

#### 4.3.1 功能说明

当存在新版本但当前版本仍可使用（current ≥ min_version 且 current < latest）时，展示可关闭的提示弹窗。

#### 4.3.2 界面与展示规则

| 元素 | 展示规则 | 备注 |
|------|---------|------|
| 弹窗标题 | 居中，16px 加粗 | 取 update_title |
| 版本号 | 标题下方 | 如「V1.2.0」 |
| 更新说明 | 多行文本 | 取 update_content |
| 立即更新按钮 | 主按钮 | — |
| 暂不更新按钮 | 次按钮 | 点击关闭弹窗 |
| 关闭图标 | 右上角 | 点击关闭弹窗 |

#### 4.3.3 业务逻辑与边界条件

| 步骤 | 操作 | 前置条件 | 异常/校验 | 提示文案 |
|------|------|---------|----------|---------|
| 1 | 展示非强制弹窗 | 需要更新且非强制 | 当天已点过「暂不更新」 | 不展示 |
| 2 | 点击立即更新 | — | — | 跳转下载 |
| 3 | 点击暂不更新 | — | — | 关闭弹窗，记录本地「今日不再提示」 |
| 4 | 点击关闭图标 | — | — | 同「暂不更新」 |

### 4.4 下载与安装

#### 4.4.1 功能说明

点击「立即更新」后，按平台跳转：
- iOS：跳转 App Store 对应页面。
- Android：跳转应用市场（如已接入）；未接入市场则打开浏览器下载 APK。

#### 4.4.2 业务逻辑与边界条件

| 步骤 | 操作 | 前置条件 | 异常/校验 | 提示文案 |
|------|------|---------|----------|---------|
| 1 | 识别平台 | 点击更新 | — | — |
| 2 | iOS 跳转 App Store | url 有效 | 跳转失败 | Toast「请前往 App Store 搜索绿韵家更新」 |
| 3 | Android 跳转市场 | 已配置市场包名 | 未安装对应市场 | 降级到浏览器下载 |
| 4 | Android 浏览器下载 | download_url 有效 | 下载失败 | Toast「下载失败，请检查网络后重试」 |

---

## 五、埋点需求

| 事件名称 | 触发时机 | 事件属性 | 备注 |
|---------|---------|---------|------|
| upgrade_check | 版本检测请求 | result, version | — |
| upgrade_popup_show | 更新弹窗展示 | type（force/normal） | — |
| upgrade_update_click | 点击立即更新 | type | — |
| upgrade_skip_click | 点击暂不更新 | type | — |
| upgrade_download_start | 开始下载/跳转 | platform | — |

---

## 六、接口需求

| 接口 | 方法 | 用途 | 备注 |
|------|------|------|------|
| GET /api/app/version | GET | 获取最新版本信息 | 含 force_update、download_url 等 |

---

## 七、非功能需求

- **性能**：版本检测接口响应 ≤ 500ms，不阻塞首页渲染。
- **兼容性**：支持 iOS 12+ / Android 5.0+。
- **安全**：download_url 必须为 HTTPS，域名白名单校验。
- **降级**：接口失败不影响用户继续使用当前版本。

---

## 八、验收标准

- [ ] App 启动时能正确检测版本并区分强制/非强制更新。
- [ ] 强制更新弹窗无法关闭，必须点击更新。
- [ ] 非强制更新弹窗可关闭，当天不再重复提示。
- [ ] iOS 跳转 App Store，Android 跳转市场/浏览器下载。
- [ ] 埋点事件按表格上报。

---

## 九、待确认项汇总

1. ⚠️ Android 应用市场分发渠道（华为、小米、OPPO、vivo、应用宝等）待确认。
2. ⚠️ 是否支持应用内下载安装 APK（热更新），还是仅跳转外部下载？
3. ⚠️ 版本号规则（三段式/四段式）及 min_version 维护方式待确认。
4. ⚠️ 非强制更新「今日不再提示」策略是否按自然日还是 24h 间隔？
5. ⚠️ 设置页「检查更新」入口是否在本期一并开发？
