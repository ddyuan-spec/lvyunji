# 绿韵家项目规则文档

## 验证码规则

### 规则定义
- **验证码位数**：4位数字
- **测试验证码**：`1234`

### 适用范围
此规则适用于本项目所有涉及验证码的功能模块，包括但不限于：
- 储值转账二次确认
- 其他需要手机验证码的场景

### 实现要求
1. 输入框 `maxlength="4"`
2. 验证逻辑检查 `code.length === 4`
3. 提示文案统一使用"4位验证码"

---

## 原型与UI文档协作规则

### 问题1：文案无法复制
- 原型文件中**不使用** `user-select:none`，所有文案均可复制
- 如设计同事仍无法复制，请用 `Ctrl+F5` 强制刷新清除浏览器缓存

### 问题2：功能说明与UI混淆
- 原型模式不变：继续按「UI交互预览 + 功能说明」方式生成
- 按需生成纯UI文档：当收到"根据xxx原型链接生成UI文档"指令时，单独生成一个纯UI页面（不含功能说明、测试数据）
- 纯UI文档命名格式：`ui_需求名称.html`
- 纯UI文档用途：仅供设计同事参考，不含任何交互逻辑和功能说明

### 原型文件规范
- 原型文件（如 `stored-value.html`）：包含完整交互 + 功能说明气泡 + 测试数据，供产品和开发参考
- UI文档文件（如 `ui_stored-value.html`）：纯UI展示，不含功能说明和测试数据，供设计同事参考

---

*文档创建时间：2025-05-22*
*最后更新：2025-05-25*
