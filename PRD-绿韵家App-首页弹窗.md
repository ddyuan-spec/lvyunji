# 绿韵家 App · 首页通用弹窗需求文档（PRD）

> **模块编号**：TASK-20260812-0003 ｜ **一级模块**：首页 ｜ **优先级**：P0 ｜ **端口**：App（Flutter）
> **技术栈**：Flutter ｜ **需求状态**：本期新增 ｜ **文档版本**：V1.0
> **关联文档**：`spec.md`、`kb/design-spec/lvyun-app-design-base.css`
> **原型文件**：`prototypes/home-popup.html`

---

## 一、文档基本信息

| 字段 | 内容 |
|------|------|
| 项目名称 | 绿韵家 App 重构（2026年8月期） |
| 需求模块 | 首页通用弹窗 |
| 看板任务 | TASK-20260812-0003 |
| PM 负责人 | 袁中天 |
| 技术 Owner | （⚠️ 待确认） |
| 测试负责人 | （⚠️ 待确认） |
| 文档状态 | 需求确认稿（待评审） |

### 历史修订记录

| 版本 | 日期 | 修订内容 | 状态 |
|------|------|---------|------|
| V1.0 | 2026-08-13 | 初稿：弹窗组件功能定义 + 触发逻辑 + 后台配置 + 原型 | 待评审 |

---

## 二、项目背景与目标

### 2.1 业务背景

绿韵家 App 首页是用户进入 App 后的核心流量入口。运营需要一种**灵活可控的弹窗能力**，用于在首页上向用户推送营销活动、新人福利、系统公告、版本更新等内容，以提升活动曝光率、新用户转化率和关键信息触达率。

当前小程序端无统一弹窗组件，各业务线各自实现，体验不一致且难以管理。本期在 App 端从零建设**通用弹窗组件**，由后台统一配置内容与触发规则。

### 2.2 项目目标

| 目标 | 衡量指标 |
|------|---------|
| 统一弹窗交互体验 | 所有弹窗共用一套 UI 组件与动画规范 |
| 运营可自主配置 | 后台可配置弹窗内容/类型/触发条件，无需发版 |
| 条件触发避免打扰 | 非每次进入都弹出，按规则控制展示频次 |
| 数据可追踪 | 弹窗展示/点击/关闭事件上报，支持效果分析 |

### 2.3 业务要求

- 弹窗内容由**后台配置下发**，App 端不硬编码任何弹窗文案或图片。
- 弹窗图片资源通过后台上传，App 端按 URL 拉取渲染。
- 「飘转」动效作为入场动画的框架性描述，具体动效参数（时长/曲线/角度）由设计输出动效规格后锁定（本期先实现基础缩放+微旋转方案）。
- 本期仅覆盖**首页**场景，后续可扩展至其他页面（数智/我的等），架构需预留扩展点。

---

## 三、业务流程与架构

### 3.1 弹窗展示流程

```
用户进入首页
    │
    ▼
调用弹窗接口（GET /api/popup/config）
    │
    ├── 接口异常 / 超时 → 不展示弹窗（静默失败）
    │
    ├── 返回空数据（无生效弹窗）→ 不展示
    │
    └── 返回有效弹窗配置
         │
         ▼
    校验本地条件（"不再提示"记录 / 今日已展示次数）
         │
         ├── 不满足条件 → 不展示
         │
         └── 满足条件
              │
              ▼
         渲染弹窗（飘转入场动画）
              │
              ├── 点击主按钮 → 执行对应跳转/动作 + 上报
              ├── 点击次按钮 / 关闭按钮 → 关闭弹窗 + 上报
              ├── 点击遮罩层 → 关闭弹窗 + 上报
              ├── 勾选「不再提示」→ 记录本地 + 上报后端
              └── 倒计时结束 → 自动关闭 + 上报
```

### 3.2 弹窗状态机

| 状态 | 触发条件 | 说明 |
|------|---------|------|
| 隐藏 | 初始态 / 用户关闭 / 倒计时结束 | 不占用屏幕空间 |
| 入场动画中 | 接口返回有效配置 + 条件满足 | 飘转动效播放中，遮罩已显示 |
| 展示中 | 动画结束 | 弹窗完全可见，等待用户操作 |
| 关闭动画中 | 用户主动关闭 / 倒计时结束 | 反向动画（可选，本期可用淡出替代） |

### 3.3 触发条件规则（后端控制）

弹窗是否展示由**后端接口返回 + 本地条件校验**共同决定：

| 条件维度 | 说明 | 控制方 |
|---------|------|--------|
| 生效时间 | 弹窗配置的开始时间 ~ 结束时间 | 后台配置 |
| 用户分层 | 全部用户 / 新用户（注册 ≤ N 天）/ 老用户 / 特定会员等级 | 后台配置 |
| 展示频次 | 每次进入都展示 / 每日 N 次 / 仅一次（生命周期内一次） | 后台配置 |
| 用户标签 | （预留）基于用户画像标签定向投放 | 后台配置 |
| 本地"不再提示" | 用户勾选后记录 localStorage，后续不再展示该弹窗 ID | 本地 + 上报同步 |

---

## 四、功能需求详情

### 4.1 首页弹窗 - 展示

#### 4.1.1 功能说明

当满足触发条件时，在首页内容层上方展示一个**全屏遮罩 + 居中弹窗**。弹窗包含图片区域（可选）、标题、副标题、描述文字、操作按钮组，以及底部的「不再提示」选项。

#### 4.1.2 显示位置

- **遮罩层**：覆盖整个屏幕（含状态栏和底部 TabBar）
- **弹窗容器**：水平垂直居中，距离屏幕边缘至少 37px 安全区域
- **层级（z-index）**：高于首页所有内容（z-index: 100）

#### 4.1.3 权限

- 所有登录用户均可看到弹窗（具体展示哪个弹窗由后端根据用户属性判断）
- 未登录用户也可看到部分弹窗（如新人福利、公告），由后台配置决定

#### 4.1.4 前置条件

- 用户已进入首页（首页数据加载完成后延迟展示，避免阻塞首屏渲染）
- 弹窗接口已返回有效配置数据

#### 4.1.5 逐步操作逻辑

1. **首页加载完成** → 延迟 500ms 调用弹窗配置接口 `GET /api/popup/config`（携带用户 token，未登录可不带）
2. **接口返回成功** → 解析返回的弹窗配置列表，取**优先级最高且满足条件的第一条**
3. **校验本地条件**：
   - 检查 localStorage 中该弹窗 ID 的 `no_prompt` 标记 → 若存在则跳过
   - 检查今日该弹窗已展示次数 → 若已达上限则跳过
4. **条件满足** → 构建弹窗 DOM 并添加到页面，触发入场动画
5. **入场动画**：
   - 初始状态：`scale(0.3) rotate(-15deg) translateY(-60px)`，`opacity: 0`
   - 结束状态：`scale(1) rotate(0deg) translateY(0)`，`opacity: 1`
   - 动画时长：500ms，缓动函数：`cubic-bezier(.34, 1.56, .64, 1)`（弹性曲线）
6. **动画结束后** → 弹窗进入「展示中」状态，等待用户操作

**异常分支**：

| 异常场景 | 处理方式 | 提示文案 |
|---------|---------|---------|
| 接口请求超时（>3s） | 静默失败，不展示弹窗 | 无提示 |
| 接口返回错误码 | 静默失败，不展示弹窗 | 无提示 |
| 返回数据格式异常 | 静默失败，不展示弹窗 | 控制台打印 warning 日志 |
| 图片 URL 加载失败 | 图片区域显示占位图（品牌色渐变背景 + 图标） | 不影响弹窗其他部分展示 |
| 动画不支持（低端机型） | 降级为直接显示（无动画） | 无感知 |

### 4.2 首页弹窗 - 内容结构

#### 4.2.1 功能说明

弹窗内容全部由后台配置下发，支持以下内容类型/布局模式。

#### 4.2.2 弹窗内容字段表

| 字段名称 | 类型 | 必填 | 需求说明 | 备注 |
|---------|------|------|---------|------|
| popup_id | string | 是 | 弹窗唯一标识 | 后端生成，如 `pop_20260813_001` |
| type | enum | 是 | 弹窗类型：`promo`(营销活动) / `newbie`(新人福利) / `notice`(公告通知) / `update`(版本更新) / `custom`(自定义) | 决定默认样式模板 |
| title | string | 是 | 主标题 | 最大 20 字 |
| subtitle | string | 否 | 副标题 | 最大 30 字 |
| description | string | 否 | 描述文字 | 最大 100 字，支持 `\n` 换行 |
| image_url | string | 否 | 弹窗图片 URL | 为空时隐藏图片区域；建议尺寸 600×360px |
| badge_text | string | 否 | 左上角角标文字 | 如"限时""新人""NEW"；为空时不显示 |
| badge_type | enum | 否 | 角标样式：`default`(品牌绿) / `warning`(橙红) / `highlight`(黄) | 默认 default |
| main_btn_text | string | 是 | 主按钮文字 | 最大 10 字 |
| main_btn_action | object | 是 | 主按钮动作配置（见 4.2.3） | — |
| secondary_btn_text | string | 否 | 次按钮文字 | 为空时不显示次按钮 |
| secondary_btn_action | object | 否 | 次按钮动作配置 | — |
| show_no_prompt | bool | 是否显示"不再提示" | 默认 true |
| countdown_seconds | int | 否 | 倒计时自动关闭秒数 | 0 = 不倒计时；最大 30 |
| priority | int | 是 | 优先级（数值越大越优先） | 用于多弹窗排序 |
| start_time | datetime | 是 | 生效开始时间 | — |
| end_time | datetime | 是 | 生效结束时间 | — |
| target_audience | object | 是 | 目标受众配置（见 4.3） | — |
| freq_limit | object | 是 | 频次限制配置（见 4.3） | — |
| status | enum | 是 | 状态：`enabled`(启用) / `disabled`(禁用) | — |

#### 4.2.3 按钮动作配置（action 对象）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| action_type | enum | 是 | `navigate`(跳转页面) / `close`(仅关闭) / `deeplink`(深度链接) / `h5`(打开 H5 页面) / `copy`(复制文本) |
| target | string | 否 | action_type 对应的目标值 | navigate=页面路径；h5=URL；copy=复制文本 |
| params | object | 否 | 额外参数 | 如商品 ID、活动 ID 等 |

**动作类型示例**：

```json
// 跳转到活动页
{"action_type": "navigate", "target": "/pages/activity/detail", "params": {"activity_id": "A20260813001"}}

// 打开 H5
{"action_type": "h5", "target": "https://lvyunji.com/promo/summer-sale"}

// 仅关闭弹窗
{"action_type": "close"}

// 复制优惠码
{"action_type": "copy", "target": "SAVE20"}
```

### 4.3 首页弹窗 - 触发规则配置

#### 4.3.1 目标受众（target_audience）

| 字段 | 类型 | 说明 |
|------|------|------|
| scope | enum | `all`(全部用户) / `new_user`(新用户) / `old_user`(老用户) / `member_level`(指定会员等级) / `tag`(指定标签) |
| register_within_days | int | scope=new_user 时，注册 N 天内视为新用户 | 默认 7 |
| min_level | int | scope=member_level 时，最低会员等级 | — |
| tags | string[] | scope=tag 时，用户标签列表 | 预留 |

#### 4.3.2 频次限制（freq_limit）

| 字段 | 类型 | 说明 |
|------|------|------|
| mode | enum | `every`(每次) / `daily_n`(每日 N 次) / `once`(生命周期一次) / `once_per_session`(每次启动一次) |
| daily_max | int | mode=daily_n 时，每日最大展示次数 | 默认 1 |

### 4.4 首页弹窗 - 交互行为

#### 4.4.1 关闭弹窗

用户可通过以下方式关闭弹窗：

| 关闭方式 | 行为 | 是否上报 |
|---------|------|---------|
| 点击右上角 ✕ 按钮 | 立即关闭（反向动画/淡出） | 是，event=close |
| 点击底部次按钮（"稍后再说"等） | 立即关闭 | 是，event=secondary_click |
| 点击遮罩层（弹窗外区域） | 立即关闭 | 是，event=mask_click |
| 倒计时归零 | 自动关闭 | 是，event=countdown_close |

#### 4.4.2 "不再提示"

- 勾选后，将该弹窗 ID 写入 localStorage（key: `popup_no_prompt_{popup_id}` = timestamp）
- 同时调用接口 `POST /api/popup/no-prompt` 上报后端（后端记录到用户维度）
- 勾选后再关闭弹窗 → 该弹窗在本次生命周期内不再出现
- 下次进入 App 时，接口仍可能返回该弹窗配置，但前端检测到本地标记后自动跳过
- ⚠️ **用户可在「设置 → 通知管理」中重置所有"不再提示"记录**（二期）

#### 4.4.3 主按钮点击

- 根据 `main_btn_action.action_type` 执行对应动作
- 动作执行前先上报点击事件
- 动作执行后关闭弹窗

### 4.5 首页弹窗 - 接口定义

#### 4.5.1 获取弹窗配置

**请求**：
```
GET /api/popup/config
Headers: Authorization: Bearer {token}（未登录可不传）
```

**响应（200 OK）**：
```json
{
  "code": 0,
  "data": {
    "popups": [
      {
        "popup_id": "pop_20260813_001",
        "type": "promo",
        "title": "超级品牌日",
        "subtitle": "全场低至 5 折起",
        "description": "精选好物限时特惠",
        "image_url": "https://cdn.lvyunji.com/popup/brand-day.png",
        "badge_text": "限时",
        "badge_type": "warning",
        "main_btn_text": "立即抢购",
        "main_btn_action": {
          "action_type": "navigate",
          "target": "/pages/activity/detail",
          "params": {"activity_id": "A20260813001"}
        },
        "secondary_btn_text": "稍后再说",
        "secondary_btn_action": {"action_type": "close"},
        "show_no_prompt": true,
        "countdown_seconds": 0,
        "priority": 100,
        "start_time": "2026-08-13T00:00:00+08:00",
        "end_time": "2026-08-20T23:59:59+08:00",
        "target_audience": {"scope": "all"},
        "freq_limit": {"mode": "daily_n", "daily_max": 3},
        "status": "enabled"
      }
    ]
  }
}
```

**说明**：
- 返回 `popups` 数组，前端取 **priority 最高且当前时间在 [start_time, end_time] 内且 status=enabled** 的第一条
- 未登录时 `target_audience.scope=all` 或 `scope=new_user` 的弹窗也可能返回（用于新人引导）
- 无有效弹窗时返回 `{"code": 0, "data": {"popups": []}}`

#### 4.5.2 上报"不再提示"

**请求**：
```
POST /api/popup/no-prompt
Headers: Authorization: Bearer {token}, Content-Type: application/json
Body: { "popup_id": "pop_20260813_001" }
```

**响应（200 OK）**：`{"code": 0}`

#### 4.5.3 弹窗事件埋点

每次弹窗的关键行为均需上报埋点（复用绿韵家埋点体系，事件前缀 `popup_`）：

| 事件名 | 触发时机 | 上报参数 |
|--------|---------|---------|
| `popup_show` | 弹窗展示（动画结束时） | popup_id, type, title |
| `popup_click_main` | 点击主按钮 | popup_id, action_type |
| `popup_click_secondary` | 点击次按钮 | popup_id |
| `popup_close` | 点击关闭按钮 | popup_id |
| `popup_mask_click` | 点击遮罩关闭 | popup_id |
| `popup_countdown_close` | 倒计时自动关闭 | popup_id |
| `popup_no_prompt` | 勾选"不再提示" | popup_id |

---

## 五、非功能需求

### 5.1 性能要求

- 弹窗接口请求**不阻塞首页渲染**，异步加载
- 弹窗图片使用**懒加载**，进入动画开始前预加载；加载失败降级为纯色占位
- 弹窗 DOM 在关闭后**延迟 300ms 再移除**（等待关闭动画完成）
- 接口响应 < 500ms（P95）；超时阈值 3s

### 5.2 兼容性

- iOS 14+ / Android 8+
- 低端机型（< 3GB RAM）：禁用飘转动效，直接展示（降级策略）
- 横屏模式：弹窗强制居中，宽度不超过屏幕宽度的 85%

### 5.3 安全

- 弹窗图片 URL 必须**白名单校验**，防止 XSS
- 按钮动作 `navigate` target 必须为**应用内合法路径**，禁止跳转外部 Scheme（除非显式配置 h5/deeplink）
- `no-prompt` 接口必须校验 token，防止伪造

---

## 六、后台配置功能（配套）

> ⚠️ 后台原型/PRD 另出本文档（ TASK-待建 ），此处仅列出后台需要提供的核心配置能力。

### 6.1 弹窗管理后台核心字段

| 配置项 | 说明 |
|--------|------|
| 弹窗名称 | 运营内部命名，如"8月品牌日活动弹窗" |
| 弹窗类型 | promo / newbie / notice / update / custom |
| 生效时间 | 开始时间 ~ 结束时间 |
| 受众范围 | 全部 / 新用户 / 老用户 / 指定会员等级 |
| 频次控制 | 每次进入 / 每日N次 / 仅一次 |
| 内容编辑 | 标题 / 副标题 / 描述 / 图片上传 / 角标 |
| 按钮配置 | 主按钮文字 + 动作 / 次按钮文字 + 动作 |
| 倒计时 | 秒数（0=不倒计时） |
| 优先级 | 数值越大越优先 |
| 启用/停用 | 开关 |

### 6.2 数据预览

- 支持手机端预览弹窗实际效果
- 支持按用户身份模拟（新用户/老用户/VIP）预览不同受众看到的弹窗

---

## 七、测试要点

| 测试项 | 测试方法 | 预期结果 |
|--------|---------|---------|
| 正常展示 | 配置一条生效弹窗，满足条件的用户进入首页 | 弹窗正常展示，飘转动效流畅 |
| 多弹窗优先级 | 同时配置多条生效弹窗，priority 不同 | 只展示 priority 最高的那条 |
| 时间边界 | 当前时间恰好在 start_time / end_time 边界 | 边界值精确匹配，不出现提前/延迟展示 |
| 频次限制 | 设置 daily_max=1，同一天第二次进入首页 | 第二次不展示 |
| 不再提示 | 勾选后重新进入首页 | 不再展示该弹窗 |
| 接口超时 | mock 接口 5s 不返回 | 静默失败，首页正常使用 |
| 图片加载失败 | 配置一个无效图片 URL | 显示占位图，弹窗其余部分正常 |
| 低端机型降级 | 在低性能设备上查看 | 无动画卡顿，弹窗正常展示和交互 |
| 埋点上报 | 操作弹窗后检查埋点日志 | 所有关键事件均有上报 |

---

## 八、待确认项

| # | 待确认项 | 涉及干系人 | 状态 |
|---|---------|-----------|------|
| 1 | 弹窗接口路径与鉴权方式（沿用现有 token 体系？） | @宋健 | ⏳ 待确认 |
| 2 | 弹窗图片 CDN 存储方案与白名单域名 | @宋健 | ⏳ 待确认 |
| 3 | 飘转动效的具体参数（时长/角度/曲线）是否采用原型中的默认值？ | @设计 | ⏳ 待确认 |
| 4 | "不再提示"的重置入口放在哪里？（设置→通知管理？） | @产品 | ⏳ 待确认 |
| 5 | 后台弹窗管理功能是否纳入本期范围？还是先用数据库/API 手动配置？ | @袁中天 | ⏳ 待确认 |
| 6 | 弹窗是否需要支持多张图片轮播？（如多活动轮播） | @运营 | ⏳ 待确认 |

---

## 九、附录

### 9.1 弹窗类型与推荐用法对照

| 类型 | 适用场景 | 推荐配置 |
|------|---------|---------|
| `promo` | 营销活动、促销、大促 | 大图 + 主CTA按钮 + 倒计时 + "稍后再说" |
| `newbie` | 新人礼包、注册引导 | 强调色图片 + "立即领取" + "不再提示" |
| `notice` | 系统公告、服务变更 | 无图 + 纯文字 + "我知道了" |
| `update` | 版本更新提醒 | 品牌色图片 + "立即更新" + "暂不更新" |
| `custom` | 其他自定义场景 | 全字段可配 |

### 9.2 原型文件说明

- **原型路径**：`prototypes/home-popup.html`
- **查看方式**：浏览器直接打开即可交互
- **右上角控制面板**：可切换 4 种弹窗类型（营销活动/新人福利/公告通知/版本更新），模拟不同配置效果
- **飘转动效**：缩放 0.3→1 + 旋转 -15°→0° + 上移 -60px→0，弹性缓动曲线
